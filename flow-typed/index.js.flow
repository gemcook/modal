// @flow
import type {ElementType, Component} from 'react';

type OnClickEvent = SyntheticEvent<HTMLButtonElement>;
type OnClick<T> = (event: T) => void;

declare export function Buttons(
  props: {} & {
    isLoading?: boolean,
    isShowNoButton?: boolean,
    isShowYesButton?: boolean,
    noHandler?: OnClick<OnClickEvent>,
    noLabel?: string,
    yesHandler?: OnClick<OnClickEvent>,
    yesLabel?: string,
  },
): $Exact<{
  +key: string | number | null,
  +props: any /* UNEXPECTED TYPE: EvalT */,
  +ref: any,
  +type: ElementType,
}>;

declare export var Modal: Class<Component<any, any>>;
