/* @flow */
export type Props = {
  isModal: boolean,
  handleCloseModal: (isModal: boolean) => boolean,
  size: string,
  height: number,
  closeButton: boolean,
  ModalBody: any,
  isCloseButton: boolean,
};
